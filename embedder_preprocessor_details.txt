- To test run this:
    + Create a directory called "source_materials" and place all PDFs inside
    + Install libraries: PyMuPDF, transformers, Pillow, re, os, globe
    + Run python test_embedder.py

- The example of how to run individual scripts can also be found inside test_embedder and those script
- In addition to the functions of interest to run in the main pipeline, there are also extra functions leftover from 
development that aren't necessary anymore that can be used if needed.

P.S. For the U I A mofo: the function you requested for will be embed_img(image_path, caption) in a dedicated python file for you, 
where that is you'll be able to figure out.